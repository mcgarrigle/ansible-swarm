BRING UP CLUSTER

  # cloud up
  # cloud inventory > inventory.yaml

or:

  create virtualbox VMs and edit inventory.yaml

```
ansible -i inventory.yaml all -b -a 'dnf update -y'          
ansible -i inventory.yaml all -b -a 'reboot'
```
then:

```
ansible-playbook -i inventory.yaml site.yaml
```
