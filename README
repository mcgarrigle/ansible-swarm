BRING UP CLUSTER

  # cloud up
  # cloud inventory > inventory.yaml

or:

  create virtualbox VMs and edit inventory.yaml

then:

  # ansible-galaxy collection install ansible.posix
  # ansible-playbook -i inventory.yaml docker-swarm.yaml
